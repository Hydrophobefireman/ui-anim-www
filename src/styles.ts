import { css } from "catom";

export const center = css({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
});

export const link = css({
  color: "inherit",
});

export const code = css({
  backgroundColor: "#d8d6d69c",
  textTransform: "none",
  borderRadius: "5px",
  padding: "3px",
});

export const codePre = css({ fontSize: ".85rem", overflow: "auto" });
